module.exports = function(app) {
    // var Game = app.models.Game;
    //
    // const find = Game.find;
    // Game.find({include: 'user'}, function (filter, cb) {
    //     return find.apply(this, arguments);
    // });
}